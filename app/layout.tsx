import type React from "react"
import type { Metadata } from "next"
import { GeistSans } from "geist/font/sans"
import { GeistMono } from "geist/font/mono"
import { Analytics } from "@vercel/analytics/next"
import { Suspense } from "react"
import "./globals.css"

export const metadata: Metadata = {
  title: "Captibiz - Professional Business Plans & Financial Modeling",
  description:
    "Expert business plan writing, financial projections, and modeling services for startups, loans, and investor visas. Trusted by entrepreneurs across the US.",
  generator: "Captibiz",
  keywords:
    "business plan, financial modeling, startup consulting, investor visa, loan application, business projections",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans ${GeistSans.variable} ${GeistMono.variable}`}>
        <Suspense fallback={null}>{children}</Suspense>
        <Analytics />
      </body>
    </html>
  )
}
